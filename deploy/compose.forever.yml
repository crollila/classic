services:
  forever-sim:
    build:
      context: ../..
      dockerfile: classic/deploy/Dockerfile.forever
    ports:
      - "127.0.0.1:8088:8080"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
      - /tmp
